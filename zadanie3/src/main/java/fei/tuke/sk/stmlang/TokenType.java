package fei.tuke.sk.stmlang;

public enum TokenType {
    // keywords
    COMMANDS, EVENTS, RESET_COMMANDS, STATE, ACTIONS,
    // syntax
    LBRACKET, RBRACKET, QUOTE, ASSIGN, NAME,
    // others
    EOF
}